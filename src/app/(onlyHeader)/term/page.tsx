/**
 * @file page.tsx
 * @description 이용약관 페이지 컴포넌트
 * Term 컴포넌트를 렌더링하여 사용자 약관 기능을 제공
 */

import Term from '@/components/etc/Term';

/**
 * @description 이용약관 페이지 컴포넌트. Term 컴포넌트를 렌더링
 * @return {TSX.Element} 이용약관 UI
 */
export default function termPage() {
	return <Term />;
}
